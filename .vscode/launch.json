{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Web APP",
            "request": "launch",
            "type": "msedge",
            "url": "http://localhost:5500/",
            "webRoot": "${workspaceFolder}"
        }
    ]
}